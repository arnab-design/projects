
  # Restaurant Menu Design

This is a code bundle for Restaurant Menu Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  